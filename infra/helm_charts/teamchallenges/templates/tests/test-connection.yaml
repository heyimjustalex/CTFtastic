apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teamchallenges.fullname" . }}-test-connection"
  labels:
    {{- include "teamchallenges.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teamchallenges.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
