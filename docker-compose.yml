version: "3.8"
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend/src:/app/src:ro
      - /app/node_modules
    ports:
      - 80:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
